diff --git a/src/components/Admin/AdminScreen.css b/src/components/Admin/AdminScreen.css
index 2edd34e..af1d94c 100644
--- a/src/components/Admin/AdminScreen.css
+++ b/src/components/Admin/AdminScreen.css
@@ -15,6 +15,33 @@
   margin-bottom: var(--space-4);
 }
 
+.admin__tabs {
+  display: flex;
+  gap: var(--space-2);
+  margin-bottom: var(--space-4);
+  overflow-x: auto;
+  -webkit-overflow-scrolling: touch;
+  scrollbar-width: none;
+}
+.admin__tabs::-webkit-scrollbar {
+  display: none;
+}
+
+.admin__tab {
+  flex: 0 0 auto;
+  white-space: nowrap;
+  background: var(--line-100);
+  color: var(--ink-500);
+  padding: 10px 14px;
+  border-radius: var(--radius-sm);
+  font-weight: 700;
+  font-size: 12.5px;
+}
+.admin__tab[aria-selected='true'] {
+  background: var(--navy-800);
+  color: var(--white);
+}
+
 .admin__stats {
   margin-bottom: 20px;
 }
diff --git a/src/components/Admin/AdminScreen.jsx b/src/components/Admin/AdminScreen.jsx
index b437d80..b986926 100644
--- a/src/components/Admin/AdminScreen.jsx
+++ b/src/components/Admin/AdminScreen.jsx
@@ -1,3 +1,4 @@
+import { useState } from 'react';
 import { useFirestoreData } from '../../firebase/DataProvider.jsx';
 import { computeStatCards, computeEstacaDist, computeBarrioDist } from '../../domain/stats.js';
 import StatCards from '../shared/StatCards.jsx';
@@ -6,8 +7,15 @@ import TablesConfig from './TablesConfig.jsx';
 import UsersConfig from './UsersConfig.jsx';
 import './AdminScreen.css';
 
+const TABS = [
+  { id: 'dashboard', label: 'Dashboard' },
+  { id: 'mesas', label: 'Mesas' },
+  { id: 'usuarios', label: 'Usuarios' },
+];
+
 export default function AdminScreen() {
   const { participants, tables, loading } = useFirestoreData();
+  const [tab, setTab] = useState('dashboard');
 
   if (loading) {
     return (
@@ -22,17 +30,35 @@ export default function AdminScreen() {
       <h2 className="admin__title">Dashboard — Admin</h2>
       <div className="admin__subtitle">Actualización en tiempo real · La Velada 2026</div>
 
-      <div className="admin__stats">
-        <StatCards cards={computeStatCards(participants, tables)} />
+      <div className="admin__tabs" role="tablist" aria-label="Secciones de administración">
+        {TABS.map((t) => (
+          <button
+            key={t.id}
+            type="button"
+            role="tab"
+            aria-selected={tab === t.id}
+            className="admin__tab press"
+            onClick={() => setTab(t.id)}
+          >
+            {t.label}
+          </button>
+        ))}
       </div>
 
-      <div className="admin__dist-row">
-        <DistributionBars title="Avance por estaca" rows={computeEstacaDist(participants)} color="var(--navy-800)" />
-        <DistributionBars title="Detalle por barrio" rows={computeBarrioDist(participants)} color="var(--gold-500)" scroll />
-      </div>
+      {tab === 'dashboard' && (
+        <>
+          <div className="admin__stats">
+            <StatCards cards={computeStatCards(participants, tables)} />
+          </div>
+          <div className="admin__dist-row">
+            <DistributionBars title="Avance por estaca" rows={computeEstacaDist(participants)} color="var(--navy-800)" />
+            <DistributionBars title="Detalle por barrio" rows={computeBarrioDist(participants)} color="var(--gold-500)" />
+          </div>
+        </>
+      )}
 
-      <TablesConfig tables={tables} />
-      <UsersConfig />
+      {tab === 'mesas' && <TablesConfig tables={tables} />}
+      {tab === 'usuarios' && <UsersConfig />}
     </div>
   );
 }
diff --git a/src/components/Admin/TablesConfig.css b/src/components/Admin/TablesConfig.css
index 8112d21..00004ec 100644
--- a/src/components/Admin/TablesConfig.css
+++ b/src/components/Admin/TablesConfig.css
@@ -1,11 +1,16 @@
 .tables-config__row {
-  display: flex;
-  flex-wrap: wrap;
-  gap: 8px;
-  align-items: flex-end;
-  padding: 8px 4px;
-  border-bottom: 1px solid var(--cream-50);
-  border-radius: 6px;
+  display: grid;
+  grid-template-columns: 1fr auto auto;
+  grid-template-areas:
+    'name occ delete'
+    'cap  cap  cap'
+    'res  res  res';
+  gap: var(--space-2);
+  align-items: center;
+  padding: var(--space-3);
+  border: 1px solid var(--line-100);
+  border-radius: var(--radius-md);
+  margin-bottom: var(--space-2);
   transition: background-color var(--motion-base) var(--ease-out);
 }
 
@@ -18,46 +23,47 @@
 }
 
 .tables-config__name {
-  flex: 1 1 90px;
+  grid-area: name;
   font-weight: 700;
   color: var(--navy-800);
-  font-size: 13.5px;
-  align-self: center;
+  font-size: 14.5px;
+  font-family: var(--font-display);
 }
 
 .tables-config__field {
   display: flex;
   flex-direction: column;
-  gap: 2px;
+  gap: 3px;
 }
 .tables-config__field-cap {
-  flex: 1 1 90px;
+  grid-area: cap;
 }
 .tables-config__field-res {
-  flex: 1 1 150px;
+  grid-area: res;
 }
 
 .tables-config__field-label {
-  font-size: 10px;
+  font-size: 10.5px;
   color: var(--ink-200);
+  font-weight: 600;
 }
 
 .tables-config__field input,
 .tables-config__field select {
-  padding: 7px 8px;
+  padding: 9px 10px;
   border-radius: 7px;
-  font-size: 13px;
+  font-size: 13.5px;
   width: 100%;
-  min-height: 38px;
+  min-height: var(--touch-min);
 }
 
 .tables-config__occ {
+  grid-area: occ;
   display: flex;
   flex-direction: column;
   gap: 4px;
   font-size: 12.5px;
   color: var(--ink-300);
-  align-self: center;
   min-width: 64px;
 }
 
@@ -78,9 +84,9 @@
 }
 
 .tables-config__delete {
+  grid-area: delete;
   background: none;
   color: var(--danger-500);
-  align-self: center;
   min-height: var(--touch-min);
   min-width: var(--touch-min);
 }
diff --git a/src/components/NavBar.css b/src/components/NavBar.css
index 3d3633e..f83fc5a 100644
--- a/src/components/NavBar.css
+++ b/src/components/NavBar.css
@@ -1,4 +1,7 @@
 .navbar {
+  position: sticky;
+  top: 0;
+  z-index: 20;
   background: var(--navy-800);
   display: flex;
   align-items: center;
diff --git a/src/components/Reception/RecentActivity.css b/src/components/Reception/RecentActivity.css
index d74a37e..341dafe 100644
--- a/src/components/Reception/RecentActivity.css
+++ b/src/components/Reception/RecentActivity.css
@@ -33,6 +33,18 @@
   max-width: 240px;
 }
 
+.recent-activity__select {
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+  align-items: flex-start;
+  gap: 4px;
+  background: none;
+  padding: 0;
+  min-width: 0;
+  text-align: left;
+}
+
 .recent-activity__name {
   font-size: 12.5px;
   font-weight: 700;
@@ -40,6 +52,7 @@
   white-space: nowrap;
   overflow: hidden;
   text-overflow: ellipsis;
+  max-width: 140px;
 }
 
 .recent-activity__undo {
@@ -48,8 +61,7 @@
   color: var(--danger-500);
   font-size: 11.5px;
   font-weight: 700;
-  padding: 4px 8px;
-  min-height: 30px;
+  padding: 4px 10px;
   border-radius: 6px;
   white-space: nowrap;
 }
diff --git a/src/components/Reception/RecentActivity.jsx b/src/components/Reception/RecentActivity.jsx
index 37dbe89..bb3fdea 100644
--- a/src/components/Reception/RecentActivity.jsx
+++ b/src/components/Reception/RecentActivity.jsx
@@ -25,8 +25,7 @@ export default function RecentActivity({ participants, onSelect }) {
 
   if (recent.length === 0) return null;
 
-  async function handleUndo(p, e) {
-    e.stopPropagation();
+  async function handleUndo(p) {
     setBusyId(p.id);
     try {
       if (p.status === 'asignado') {
@@ -48,16 +47,16 @@ export default function RecentActivity({ participants, onSelect }) {
       <div className="recent-activity__title">Actividad reciente</div>
       <div className="recent-activity__row">
         {recent.map((p) => (
-          <div key={p.id} className="recent-activity__card press" onClick={() => onSelect(p.id)} role="button" tabIndex={0}>
-            <div>
+          <div key={p.id} className="recent-activity__card">
+            <button type="button" className="recent-activity__select press" onClick={() => onSelect(p.id)}>
               <div className="recent-activity__name">
                 {p.nombre} {p.apellidos}
               </div>
               <span className="badge" style={{ background: STATUS_META[p.status].bg, color: STATUS_META[p.status].color }}>
                 {STATUS_META[p.status].label}
               </span>
-            </div>
-            <button type="button" className="recent-activity__undo" disabled={busyId === p.id} onClick={(e) => handleUndo(p, e)}>
+            </button>
+            <button type="button" className="recent-activity__undo press" disabled={busyId === p.id} onClick={() => handleUndo(p)}>
               Deshacer
             </button>
           </div>
diff --git a/src/components/Reception/ReceptionScreen.css b/src/components/Reception/ReceptionScreen.css
index 9f07970..e3b941b 100644
--- a/src/components/Reception/ReceptionScreen.css
+++ b/src/components/Reception/ReceptionScreen.css
@@ -26,10 +26,17 @@
   display: flex;
   gap: var(--space-2);
   margin-bottom: var(--space-4);
-  flex-wrap: wrap;
+  overflow-x: auto;
+  -webkit-overflow-scrolling: touch;
+  scrollbar-width: none;
+}
+.reception__tabs::-webkit-scrollbar {
+  display: none;
 }
 
 .reception__tab {
+  flex: 0 0 auto;
+  white-space: nowrap;
   background: var(--line-100);
   color: var(--ink-500);
   padding: 10px 14px;
diff --git a/src/components/Reception/ReceptionScreen.jsx b/src/components/Reception/ReceptionScreen.jsx
index 61094ae..cdc1a71 100644
--- a/src/components/Reception/ReceptionScreen.jsx
+++ b/src/components/Reception/ReceptionScreen.jsx
@@ -61,7 +61,7 @@ export default function ReceptionScreen() {
               <StatCards cards={computeStatCards(participants, tables)} />
               <div className="reception__dist-row">
                 <DistributionBars title="Avance por estaca" rows={computeEstacaDist(participants)} color="var(--navy-800)" />
-                <DistributionBars title="Detalle por barrio" rows={computeBarrioDist(participants)} color="var(--gold-500)" scroll />
+                <DistributionBars title="Detalle por barrio" rows={computeBarrioDist(participants)} color="var(--gold-500)" />
               </div>
             </>
           )}
diff --git a/src/components/Reception/SearchTab.css b/src/components/Reception/SearchTab.css
index aceb894..76d59c1 100644
--- a/src/components/Reception/SearchTab.css
+++ b/src/components/Reception/SearchTab.css
@@ -22,8 +22,6 @@
   display: flex;
   flex-direction: column;
   gap: var(--space-2);
-  max-height: 480px;
-  overflow-y: auto;
 }
 
 .search-tab__row {
diff --git a/src/components/Reception/SearchTab.jsx b/src/components/Reception/SearchTab.jsx
index 8b2e2c1..376201e 100644
--- a/src/components/Reception/SearchTab.jsx
+++ b/src/components/Reception/SearchTab.jsx
@@ -1,4 +1,4 @@
-import { useMemo, useState } from 'react';
+import { useEffect, useMemo, useRef, useState } from 'react';
 import { useStore } from '../../state/store.jsx';
 import { useFirestoreData } from '../../firebase/DataProvider.jsx';
 import { useToast } from '../../hooks/useToast.jsx';
@@ -14,6 +14,14 @@ export default function SearchTab() {
   const toast = useToast();
   const { search, selectedId } = state;
   const [busy, setBusy] = useState(false);
+  const detailRef = useRef(null);
+
+  // En mobile, lista y detalle quedan apilados (el detalle cae debajo de
+  // toda la lista). Sin este scroll, tocar a alguien no muestra ningún
+  // cambio visible en pantalla — el detalle aparece fuera del viewport.
+  useEffect(() => {
+    if (selectedId) detailRef.current?.scrollIntoView({ behavior: 'smooth', block: 'start' });
+  }, [selectedId]);
 
   const filtered = useMemo(() => {
     const q = search.trim().toLowerCase();
@@ -111,7 +119,7 @@ export default function SearchTab() {
         </div>
       </div>
 
-      <div className="search-tab__detail">
+      <div className="search-tab__detail" ref={detailRef}>
         {selected ? (
           <>
             <div className="search-tab__detail-name">
diff --git a/src/components/shared/DistributionBars.css b/src/components/shared/DistributionBars.css
index 04be3f1..076d43a 100644
--- a/src/components/shared/DistributionBars.css
+++ b/src/components/shared/DistributionBars.css
@@ -18,10 +18,7 @@
   flex-direction: column;
   gap: 9px;
 }
-.dist-list--scroll {
-  max-height: 220px;
-  overflow-y: auto;
-}
+
 
 .dist-row__top {
   display: flex;
diff --git a/src/components/shared/DistributionBars.jsx b/src/components/shared/DistributionBars.jsx
index a7daa04..f12d7f3 100644
--- a/src/components/shared/DistributionBars.jsx
+++ b/src/components/shared/DistributionBars.jsx
@@ -1,10 +1,10 @@
 import './DistributionBars.css';
 
-export default function DistributionBars({ title, rows, color, scroll }) {
+export default function DistributionBars({ title, rows, color }) {
   return (
     <div className="dist-panel">
       <div className="dist-panel__title">{title}</div>
-      <div className={scroll ? 'dist-list dist-list--scroll' : 'dist-list'}>
+      <div className="dist-list">
         {rows.map((r) => (
           <div key={r.label}>
             <div className="dist-row__top">
diff --git a/src/components/ui/Icon.jsx b/src/components/ui/Icon.jsx
index 232641b..9f397f6 100644
--- a/src/components/ui/Icon.jsx
+++ b/src/components/ui/Icon.jsx
@@ -1,10 +1,10 @@
-// Small inline SVG icon set — no emoji, consistent 1.75px stroke, 24px grid.
+// Small inline SVG icon set — no emoji, consistent 2px stroke, 24px grid.
 const base = { width: 20, height: 20, viewBox: '0 0 24 24', fill: 'none', 'aria-hidden': true };
 
 export function CheckIcon(props) {
   return (
     <svg {...base} {...props}>
-      <path d="M20 6L9 17l-5-5" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
+      <path d="M20 6L9 17l-5-5" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
     </svg>
   );
 }
@@ -23,7 +23,7 @@ export function AlertIcon(props) {
       <path
         d="M12 9v4m0 4h.01M10.29 3.86l-8.18 14.18A1.5 1.5 0 0 0 3.5 20.5h17a1.5 1.5 0 0 0 1.39-2.46L13.71 3.86a1.5 1.5 0 0 0-2.6 0z"
         stroke="currentColor"
-        strokeWidth="1.75"
+        strokeWidth="2"
         strokeLinecap="round"
         strokeLinejoin="round"
       />
@@ -45,7 +45,7 @@ export function TrashIcon(props) {
       <path
         d="M4 7h16M9 7V4.5A1.5 1.5 0 0 1 10.5 3h3A1.5 1.5 0 0 1 15 4.5V7m2 0v12.5A1.5 1.5 0 0 1 15.5 21h-7A1.5 1.5 0 0 1 7 19.5V7h10Z"
         stroke="currentColor"
-        strokeWidth="1.75"
+        strokeWidth="2"
         strokeLinecap="round"
         strokeLinejoin="round"
       />
diff --git a/src/components/ui/ToastHost.css b/src/components/ui/ToastHost.css
index 66ed707..e021dd1 100644
--- a/src/components/ui/ToastHost.css
+++ b/src/components/ui/ToastHost.css
@@ -1,6 +1,6 @@
 .toast-host {
   position: fixed;
-  bottom: var(--space-5);
+  bottom: calc(var(--space-5) + env(safe-area-inset-bottom, 0px));
   left: 50%;
   transform: translateX(-50%);
   z-index: 100;
@@ -34,9 +34,12 @@
 .toast button {
   margin-left: auto;
   background: none;
-  min-height: 0;
+  min-height: var(--touch-min);
+  min-width: var(--touch-min);
+  display: flex;
+  align-items: center;
+  justify-content: center;
   color: rgba(255, 255, 255, 0.75);
-  padding: 4px;
 }
 .toast button:hover {
   color: #fff;
diff --git a/src/hooks/useToast.jsx b/src/hooks/useToast.jsx
index c3b24c5..f076ac3 100644
--- a/src/hooks/useToast.jsx
+++ b/src/hooks/useToast.jsx
@@ -17,7 +17,7 @@ export function ToastProvider({ children }) {
   const push = useCallback(
     (message, tone = 'success') => {
       const id = nextId++;
-      setToasts((t) => [...t, { id, message, tone }]);
+      setToasts((t) => [...t, { id, message, tone }].slice(-3));
       const timer = setTimeout(() => dismiss(id), 4000);
       timers.current.set(id, timer);
     },
