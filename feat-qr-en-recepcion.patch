diff --git a/src/components/Admin/ActivitiesConfig.jsx b/src/components/Admin/ActivitiesConfig.jsx
index a34b581..8773113 100644
--- a/src/components/Admin/ActivitiesConfig.jsx
+++ b/src/components/Admin/ActivitiesConfig.jsx
@@ -2,7 +2,7 @@ import { useRef, useState } from 'react';
 import { useToast } from '../../hooks/useToast.jsx';
 import { createActivity, updateActivity, setActiveActivity } from '../../firebase/collections.js';
 import Field from '../ui/Field.jsx';
-import ActivityQRModal from './ActivityQRModal.jsx';
+import ActivityQRModal from '../shared/ActivityQRModal.jsx';
 import './ActivitiesConfig.css';
 
 const EMPTY = { nombre: '', fecha: '', lugar: '', anfitrion: 'Centro JAS Noroeste' };
diff --git a/src/components/Reception/ReceptionScreen.css b/src/components/Reception/ReceptionScreen.css
index 03d2604..d40b72d 100644
--- a/src/components/Reception/ReceptionScreen.css
+++ b/src/components/Reception/ReceptionScreen.css
@@ -44,3 +44,32 @@
   gap: 14px;
   margin-top: var(--space-4);
 }
+
+.reception__activity-bar {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+  background: var(--navy-800);
+  color: var(--white);
+  padding: 10px 14px;
+  border-radius: var(--radius-sm);
+  margin-bottom: var(--space-3);
+}
+.reception__activity-name {
+  font-weight: 700;
+  font-size: 13px;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+.reception__qr-btn {
+  flex-shrink: 0;
+  background: var(--gold-500);
+  color: var(--navy-800);
+  font-weight: 800;
+  font-size: 12px;
+  padding: 8px 14px;
+  border-radius: 7px;
+  min-height: var(--touch-min);
+}
diff --git a/src/components/Reception/ReceptionScreen.jsx b/src/components/Reception/ReceptionScreen.jsx
index 8a3d13d..7b63eb8 100644
--- a/src/components/Reception/ReceptionScreen.jsx
+++ b/src/components/Reception/ReceptionScreen.jsx
@@ -1,3 +1,4 @@
+import { useRef, useState } from 'react';
 import { useStore } from '../../state/store.jsx';
 import { useFirestoreData } from '../../firebase/DataProvider.jsx';
 import { computeStatCards, computeEstacaDist, computeBarrioDist } from '../../domain/stats.js';
@@ -5,6 +6,7 @@ import SearchTab from './SearchTab.jsx';
 import ManualTab from './ManualTab.jsx';
 import StatCards from '../shared/StatCards.jsx';
 import DistributionBars from '../shared/DistributionBars.jsx';
+import ActivityQRModal from '../shared/ActivityQRModal.jsx';
 import './ReceptionScreen.css';
 
 const TABS = [
@@ -17,11 +19,27 @@ export default function ReceptionScreen() {
   const { state, dispatch } = useStore();
   const { participants, loading, activeActivity } = useFirestoreData();
   const { receptionTab } = state;
+  const [qrOpen, setQrOpen] = useState(false);
+  const qrTriggerRef = useRef(null);
 
   return (
     <div className="reception">
       <h2 className="sr-only">Recepción{activeActivity ? ` — ${activeActivity.nombre}` : ''}</h2>
 
+      {/* Mismo QR que Admin → Actividades (mostrar/pantalla completa/
+          compartir/imprimir) — recepción no siempre tiene acceso a Admin,
+          pero sí necesita poder mostrarle el QR a alguien que llega sin
+          haberlo escaneado antes. Se reusa el componente entero en vez de
+          reconstruir esas cuatro acciones de nuevo (ponytail). */}
+      {activeActivity && (
+        <div className="reception__activity-bar">
+          <div className="reception__activity-name">{activeActivity.nombre}</div>
+          <button type="button" className="reception__qr-btn press" ref={qrTriggerRef} onClick={() => setQrOpen(true)}>
+            QR
+          </button>
+        </div>
+      )}
+
       <div className="reception__tabs" role="tablist" aria-label="Secciones de recepción">
         {TABS.map((t) => (
           <button
@@ -54,6 +72,8 @@ export default function ReceptionScreen() {
           )}
         </>
       )}
+
+      {qrOpen && activeActivity && <ActivityQRModal activity={activeActivity} triggerRef={qrTriggerRef} onClose={() => setQrOpen(false)} />}
     </div>
   );
 }
diff --git a/src/components/Admin/ActivityQRModal.css b/src/components/shared/ActivityQRModal.css
similarity index 100%
rename from src/components/Admin/ActivityQRModal.css
rename to src/components/shared/ActivityQRModal.css
diff --git a/src/components/Admin/ActivityQRModal.jsx b/src/components/shared/ActivityQRModal.jsx
similarity index 100%
rename from src/components/Admin/ActivityQRModal.jsx
rename to src/components/shared/ActivityQRModal.jsx
